# smartHome APP
Vue.js 2.x + Axios + Mint-UI + JQuery + Vue-awesome-mui
## 关于此APP一些说明
  这个app没有使用数据库，仅为了控制设备。在控制硬件的页面，用了后台，后台是用Python写的Restful风格的api，调用硬件的方式各不相同，所以后台代码拿出来给你也没什么用，就没有贴出来了，缓存用的Vuex（如保存登录状态）。
## APP设计思路

